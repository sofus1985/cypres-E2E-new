{
  "name": "cypress-new-one",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:addToCart": "npx cypress run --spec cypress/e2e/TestCheckOut.cy.js",
    "test:login": " npx cypress run --spec cypress/e2e/testLogin.cy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.15.2",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
